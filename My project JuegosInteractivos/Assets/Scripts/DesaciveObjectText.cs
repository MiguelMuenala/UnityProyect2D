using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DesaciveObjectText : MonoBehaviour
{
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()//Desactivar
    {
        /*if (Input.GetKey("r"))
        {
            textGameObject.SetActive(false);
        }if (Input.GetKey("t"))
        {
            textGameObject.SetActive(false);
        }*/
    }
}
